@model IEnumerable<CRM.Models.Licenses_Plant>


<div id="ShowUsers">
    <table id="TableJson" class="table table-striped table-bordered table-hover">
        <thead>
            <tr>
                <th class="text-align-center">ลำดับ</th>
                <th class="text-align-center">รหัสรุ่น</th>
                <th>ชื่อรุ่น</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @{
                int Rows = 1;
                foreach (var list in Model)
                {
                    <tr>
                        <td class="text-align-center">@Rows.</td>
                        <td class="text-align-center">@list.LicensePlantNumber</td>
                        <td >@list.LicensePlantName</td>
                        <td class="text-align-center">
                            <button class="btn btn-warning btn-xs  edit" value="@list.LicensePlantId" style="font-size:10px;" data-toggle="modal" id="edit" data-target="#ButtonEdit"><i class="fa fa-pencil"></i> แก้ไข</button>
                            <button class="btn btn-danger btn-xs   delete" value="@list.LicensePlantId" style="font-size:10px;"><i class="fa fa-eraser"></i> ลบ</button>
                        </td>
                    </tr>
                    Rows++;
                }
            }
        </tbody>
    </table>
</div>

